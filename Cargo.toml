[package]
name = "calcx"
version = "0.1.0"
edition = "2024"

[features]
default = []
clipboard = ["dep:cli-clipboard"]

[dependencies]
cli-clipboard = { version = "0.4.0", optional=true }
rustyline = { version = "18.0.0", default-features = false }
calcx_core = { path="./calcx_core/" }
